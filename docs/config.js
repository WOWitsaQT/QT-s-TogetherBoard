// Set this to your backend base URL *after* you deploy the server.
// Example: window.WS_BASE = "https://yourapp.onrender.com";
window.WS_BASE = ""; // keep empty for local dev: will default to window.location
